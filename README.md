# dock-composer

## Your task
Complete all `TODO` items in:
- `docker-compose.yml`
- `compose.test.yaml`

## Rules
- Keep service names unchanged.
- Do not delete services.
- You may add fields and sections.

## How to check your work
Run:

```bash
cd docker_compose_quiz
./check_solution.sh
```

You will see `PASS/FAIL` for each TODO.

## Expected result
When all TODOs are completed correctly, the script prints:

```text
All checks passed. Great job!
```
